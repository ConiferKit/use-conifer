{
  "name": "@conifer/sdk",
  "version": "0.1.0",
  "description": "The Conifer gateway SDK: one client, exact per-turn cost receipts, and honest migration shims from OpenRouter, Vercel AI Gateway, and Helicone.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./mcp": "./mcp/server.ts"
  },
  "bin": {
    "conifer-mcp": "./mcp/server.ts"
  },
  "files": ["src", "mcp", "cards", "README.md"],
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "engines": { "node": ">=22" },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "license": "UNLICENSED"
}
